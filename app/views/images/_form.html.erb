<%= form_for @image, remote: true, :url => images_path, :html => { :multipart => true } do |f| %>
	<%= f.text_field :description, placeholder: 'description' %>
	<%= f.file_field :attachment %>
	<%= f.submit 'Create' %>
<% end %>