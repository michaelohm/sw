// I think the reason below code doesn't work to upload pics and add it immediately to the page through ajax is because the upload takes time while the ajax request is immediate.

// $('#new_image').remove();
// $('#new_upload').show();
// $('.image_container').append('<%= j render(@image) %>');